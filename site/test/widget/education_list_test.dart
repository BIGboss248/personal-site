import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('EducationList widget test', (WidgetTester tester) async {
    // TODO: Provide mock education data and test rendering
    // await tester.pumpWidget(EducationList(...));
    // expect(find.text('School Name'), findsOneWidget);
  });
}
