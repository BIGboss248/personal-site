import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ExperienceList widget test', (WidgetTester tester) async {
    // TODO: Provide mock experience data and test rendering
    // await tester.pumpWidget(ExperienceList(...));
    // expect(find.text('Company Name'), findsOneWidget);
  });
}
